import {createSlice} from "@reduxjs/toolkit";

const initialState = [

]

const TaskSlice = createSlice({
    name: 'TasksSlice',
    initialState,
    reducers: {

    }
})

export default TaskSlice.reducer